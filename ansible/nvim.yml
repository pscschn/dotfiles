---

- name: Deploy nvim
  hosts: all
  vars_files:
    - ./vars/fonts.yml
    - ./vars/colorscheme.yml
  vars:
    dotfile_tasks:
      - fonts.yml
      - nvim.yml
#      - git.yml
  roles:
    - dotfiles
